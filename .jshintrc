{
    // Enforcing
    "bitwise": true,
    "camelcase": true,
    "curly": true,
    "eqeqeq": true,
    "immed": true,          // true: Require immediate invocations to be wrapped in parens e.g. `(function () { } ());`
    "indent": 4,
    "latedef": true,        // true: Require variables/functions to be defined before being used
    "newcap": true,         // true: Require capitalization of all constructor functions e.g. `new F()`
    "noarg": true,          // true: Prohibit use of `arguments.caller` and `arguments.callee`
    "quotmark": "single",   // Quotation mark consistency:
                            // false : do nothing (default)
                            // true : ensure whatever is used is consistent
                            // "single" : require single quotes
                            // "double" : require double quotes
    "regexp": true,
    "undef": true,          // true: Require all non-global variables to be declared (prevents global leaks)
    "unused": true,         // true: Require all defined variables be used
    "strict": true,         // true: Requires all functions run in ES5 Strict Mode
    "trailing": true,       // true: Prohibit trailing whitespaces
    //"smarttabs": true,    // true: This option suppresses warnings about mixed tabs and spaces when 
                            // the latter are used for alignmnent only. The technique is called SmartTabs.

    // Relaxing
    "esnext": true,         // true: Allow ES.next (ES6) syntax (ex: `const`)

    // Environment
    "browser": true,
    "devel": true,         // Allow console and alert
    "jquery": true,
    //"node": true,         // for code in a node server, instead of a browser 
    "globals": {
        "define": false     // for requirejs
    }
}